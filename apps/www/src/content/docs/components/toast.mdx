---
title: Toast
description: A lightweight and customizable toast notification component.
links:
  source: components/toast
  docs: /docs/components/toast
---

<ComponentPreview name="toast-basic" />

## Setup

Add `Toaster` to your app's root layout. This component is responsible for
rendering and managing the toasts.

```tsx
import { Toaster } from "@bun-ui/react/toast"

export const RootLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      {children}
      <Toaster />
    </>
  )
}
```

## Usage

The `use-toast` hook provides a function `createToast` that we can use to
display toast

```tsx
import { useToast } from "@bun-ui/react/toast"

export const ToastDemo = () => {
  const { createToast } = useToast()

  return (
    <Button
      onClick={() => {
        createToast({
          title: "Task completed",
        })
      }}
    >
      Show Toast
    </Button>
  )
}
```

## Examples

### Maximum Toasts

Pass `maxToasts` option to the `useToast` hook to change the number of toasts

<Alert variant="warning" className="mt-2">
  <AlertTitle>For accessibility reasons, we recommend limiting the number of toasts to 4 at a time (default).</AlertTitle>
</Alert>

<ComponentPreview name="toast-max" />
