---
title: Skeleton
description: A placeholder component used to indicate loading content.
links:
  source: components/skeleton
  docs: /docs/components/skeleton
---

<ComponentPreview name="skeleton-basic" />

## Usage

Import the `Skeleton` component from `@bun-ui/react` and use it in your project:

```tsx
import { Skeleton } from "@bun-ui/react"
```

```tsx
<Skeleton width={100} height={100} />
```

## Examples

### Image

The `Skeleton` component supports different variants such as `circle` and `rectangle`. Use the `variant` prop to specify the shape.

<ComponentPreview name="skeleton-image" />

### Card

<ComponentPreview name="skeleton-card" />
